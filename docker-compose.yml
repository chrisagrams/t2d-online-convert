version: '3.4'

services:
  t2donlineconvert:
    image: t2donlineconvert
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
  nginx:
    build:
     context: .
     dockerfile: ./nginx/Dockerfile
    ports:
      - 8080:80
    depends_on:
      - t2donlineconvert